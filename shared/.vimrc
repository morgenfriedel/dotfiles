" Enable line numbers
set number
set relativenumber

" Enable syntax highlighting
syntax enable

" Show matching brackets and parentheses
set showmatch

" Enable auto-indentation
set smartindent
set tabstop=4
set shiftwidth=4
set expandtab

" Enable line wrapping
set wrap

" Enable incremental search
set incsearch

" Highlight search results as you type
set hlsearch

" Show line and column number in the status line
set ruler

" Keep 5 lines of context visible above/below cursor
set scrolloff=5

" Enable mouse support
set mouse=a

" Automatically read file changes made outside Vim
set autoread

